export interface Event {
  event_id: number;
  damage_healed: number;
  gold_gain: number;
  damage_taken: number;
  max_hp_gain: number;
  max_hp_loss: number;
  floor: number;
  gold_loss: number;
  event_name: string;
  player_choice: string;
}
